
import { useEffect } from "react";
import { useNavigate } from "react-router-dom";
import HomePage from "./HomePage";

const Index = () => {
  // Either redirect to the homepage or render the HomePage component directly
  return <HomePage />;
};

export default Index;
