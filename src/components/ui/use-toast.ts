
// This file now only contains a placeholder to maintain imports in other files
// The actual toast functionality has been removed

export const useToast = () => {
  return {
    toast: (props: any) => {}, // Accept arguments but do nothing with them
    toasts: []
  };
};

export const toast = (props: any) => {}; // Accept arguments but do nothing with them
